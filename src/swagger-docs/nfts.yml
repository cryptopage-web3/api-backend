components:
  schemas:
  
    NFTTransactions:
      type: object
      properties:
        list:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
              txHash:
                type: string
              blockNumber:
                type: number              
              contract_address:
                type: string
              tokenId:
                type: string
              to:
                type: string
              from:
                type: string
        count:
          type: number
    
    NftTransactionDetails:
      type: object
      properties:
        tokenId:
          type: string
        url:
          type: string
        name:
          type: string
        description:
          type: string
        date:
          type: string